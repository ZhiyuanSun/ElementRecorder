{
  "name": "element-recorder",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome MV3 extension for full screen and visually isolated element recording.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm run build"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.28.1",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "gifenc": "^1.0.3"
  }
}
